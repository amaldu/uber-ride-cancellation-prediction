apiVersion: 1
datasources:
  - name: Uber SQLite
    type: frser-sqlite-datasource
    uid: uber_sqlite
    access: proxy
    jsonData:
      path: /var/lib/grafana/data/uber_insights.db
    editable: false
